# REPORT.he.md — השוואת ביצועים להערכת נוסחאות דינמיות

הנתונים שלהלן נאספו מהרצה בודדת של `python run_all.py`, מול מסד נתונים נקי (seed=42,
1,000,000 שורות, 13 נוסחאות, מדגם דטרמיניסטי של 10,000 שורות). לשחזור עצמאי של
התוצאות, ראו README.md. זהו לא תרגום מכונה ל-REPORT.md, אלא ניסוח טכני עצמאי בעברית,
באותה רמת דיוק.

## 1. חמש השיטות, ומדוע כל אחת מתפקדת כפי שהיא מתפקדת

### C# `ExpressionTrees` — השיטה המנצחת
כל נוסחה מפוענחת פעם אחת. מפענח recursive-descent שנכתב ידנית (ראו
`src/dotnet/PaymentSystem.Shared/FormulaParser.cs`) הופך אותה לעץ תחביר קטן (AST).
את ה-AST הזה מהדרים פעם אחת לעץ `System.Linq.Expressions`, ומקמפלים (JIT) לנציג
(delegate) native מסוג `Func<double,double,double,double,double?>`. מכאן, כל שורה
היא קריאת פונקציה native ישירה: בלי פענוח מחרוזת מחדש, בלי dispatch של אינטרפרטר,
בלי boxing מעבר לערך המוחזר (nullable). זו בדיוק אסטרטגיית "הידור פעם אחת, הרצה
מיליון פעמים" שהמטלה מבקשת. והמספרים מראים למה זו השיטה המנצחת: **844 ms** זמן
חישוב על פני 13,000,000 הערכות-שורה (13 נוסחאות × מיליון שורות) — פחות מ-65 ns
לשורה, כולל קריאת פונקציה, כמה פעולות אריתמטיות, ובדיקת NaN/אינסוף.

### T-SQL `sp_executesql`
זהו חישוב מבוסס-קבוצות (set-based) שמתבצע כולו בתוך מנוע מסד הנתונים. הנוסחה
מתורגמת לביטוי SQL בודד, עם שומרי תחום `CASE WHEN`/`NULLIF` שמוכנסים בזמן התרגום
(ראו סעיף 3), ומחושבת על כל 1,000,000 השורות בפקודת `SELECT` אחת. אף שורה לא חוצה
את הרשת בנפרד. השיטה הזו מתחרה היטב מול ExpressionTrees בזמן החישוב לנוסחה, אבל
מפסידה בשלב ה**שמירה** (6,330 ms בסך הכול). הסיבה: בניגוד לארבע השיטות האחרות, אין
לה מערך תוצאות שמור בזיכרון לשימוש חוזר. כל שמירה היא סריקה מלאה נוספת של הטבלה,
שמחשבת את הנוסחה מחדש מאפס (ראו סעיף 4). מצד שני, זו השיטה היחידה שלא משלמת שום
עלות העברת נתונים על חישוב ה-checksum על פני כל מערך הנתונים — כי החישוב הזה קורה
במקום שבו הנתונים כבר נמצאים.

### C# `DataTable.Compute`
זהו "הבסיס הנאיבי" שהמטלה מבקשת במפורש, וההצגה שלו חלשה מהצפוי — באופן שמעניין
יותר מסתם "איטי". מתברר ששפת הביטויים של `System.Data.DataColumn` **חסרה לגמרי
`Sqrt`, `Log` ו-`Pow`/`^`**. ארבע מתוך שלוש-עשרה הנוסחאות (6, 7, 12, 13 — כל נוסחה
שדורשת `sqrt` או `log`, בכל מקום, כולל בתוך תנאי) **בלתי ניתנות לביטוי** במנוע הזה.
הן מדולגות עם סיבה מתועדת, ולא מזויפות בשקט — ראו את הראיות וההסבר המלא בהמשך. את
תשע הנוסחאות שהמנוע כן מסוגל לבטא (`abs`/`min`/`max` נכתבות מחדש כביטויי `IIF`,
ו-`x^2` נכתב מחדש כ-`x*x`, כי כל חזקה בקטלוג היא בפועל המספר 2) הוא מחשב לאט: כל
קריאת `DataColumn.Compute` מפענחת את מחרוזת הביטוי מאפס. **10,868 ms זמן חישוב** —
פי 13 בערך איטי יותר לשורה מ-ExpressionTrees, כי אין שלב הידור שמפזר את העלות על
פני שורות רבות.

#### הראיות: השגיאה של ה-runtime עצמו, לא הפרשנות שלנו לה

לא הסקנו את זה מתיעוד. הזנו ל-runtime של .NET את הצורה המתורגמת המדויקת של
הנוסחאות הכושלות, ותיעדנו מה שהוא זרק בפועל. כדי לשלול שמדובר בבאג בתרגום שלנו —
למשל, אותיות רישיות שגויות — בדקנו כל וריאציה של אותיות רישיות ותחביר שאדם סביר
היה מנסה:

```
Sqrt(c * c + d * d)   -> System.Data.EvaluateException:
                          "The expression contains undefined function call Sqrt()."
sqrt(c * c + d * d)   -> System.Data.EvaluateException:
                          "The expression contains undefined function call sqrt()."
SQRT(c * c + d * d)   -> System.Data.EvaluateException:
                          "The expression contains undefined function call SQRT()."
sQrT(c * c + d * d)   -> System.Data.EvaluateException:
                          "The expression contains undefined function call sQrT()."
Sqrt (c * c + d * d)  -> System.Data.EvaluateException:
                          "The expression contains undefined function call Sqrt()."   (רווח לפני הסוגר)
Math.Sqrt(c)          -> System.Data.EvaluateException:
                          "The expression contains undefined function call Math.Sqrt()."
Log(c) / log(c) / LOG(c) / lOg(c)  -> אותה תבנית, "undefined function call {as-typed}()."
Pow(a, 2) / pow(a, 2) -> System.Data.EvaluateException:
                          "The expression contains undefined function call Pow()."
a ^ 2                 -> System.Data.EvaluateException:
                          "The expression contains unsupported operator '^'."
```

כל וריאציה נכשלת באופן זהה: כל אות רישית, עם או בלי רווח לפני הסוגר, ארגומנט בודד
או מרובה, ואפילו namespace מוקדם (`Math.Sqrt`). סוג החריגה הוא `System.Data.
EvaluateException` — טיפוס של .NET עצמו, שנזרק מתוך מפענח הביטויים של
`System.Data.dll`. שום דבר בקוד שלנו לא זורק אותו. המסקנה: זה לא באג בתרגום שלנו,
לא אותיות רישיות שגויות ולא תחביר שגוי. הפונקציה פשוט לא קיימת בדקדוק, בשום איות.

בקוד שנשלח בפועל, הידע הזה מקודד כשומר יזום — לא כתפיסת חריגה בזמן ריצה. המתרגם
דוחה `sqrt`/`log`/`pow` לא-ריבועי עוד לפני שהוא בונה `DataColumn` כלשהו. כלומר,
ה-`EvaluateException` הגולמי שלמעלה אף פעם לא באמת עולה בזמן ריצה. זו הראיה
שהצדיקה את כתיבת השומר — לא משהו שהקוד השלוח תופס ישירות:

- **מיקום זריקת השומר**: `src/dotnet/DataTableCompute/DataColumnExpressionEmitter.cs:78-81`
  (`sqrt`) ו-`:76-77`/`:66-67` (`pow`/חזקה לא-ריבועית). כל אחד זורק את
  `NotSupportedByDataTableComputeException` שלנו, עם הודעה שמציינת את הפונקציה החסרה.
- **מיקום דילוג הנוסחה**: `src/dotnet/DataTableCompute/Program.cs:60-65`. מנוע ההרצה
  תופס את החריגה הזו לכל נוסחה, רושם אותה, מדפיס `SKIPPED -- {הודעה}`, וממשיך
  לנוסחה הבאה — במקום לעצור את כל ההרצה או לזייף תוצאה.

לשחזור עצמאי: `dotnet run --project src/dotnet/DataTableCompute -c Release` —
וקריאת שורות ה-`SKIPPED` בפלט הקונסולה שלו ישירות.

#### מדוע זה קורה: `DataColumn.Expression` אינה שפת ביטויים כללית

לפי התיעוד הרשמי של Microsoft עבור `DataColumn.Expression`
([learn.microsoft.com/dotnet/api/system.data.datacolumn.expression](https://learn.microsoft.com/en-us/dotnet/api/system.data.datacolumn.expression)),
למאפיין הזה יש שלוש מטרות בלבד: "לסנן שורות, לחשב את הערכים בעמודה, או ליצור עמודת
aggregate". הדקדוק שלו הוא קבוצה קטנה וסגורה — לא ספריית מתמטיקה הניתנת להרחבה:

- **אופרטורים**: השוואה (`< > <= >= = IN LIKE`), חיבור בוליאני (`AND OR NOT`),
  אריתמטיקה (`+ - * / %`), שרשור מחרוזות (`+`).
- **פונקציות** (בדיוק שש, לפי סעיף "Functions" בתיעוד עצמו): `CONVERT`, `LEN`,
  `ISNULL`, `IIF`, `TRIM`, `SUBSTRING`.
- **Aggregates** (בדיוק שבעה, לפי סעיף "Aggregates" בתיעוד עצמו): `Sum`, `Avg`,
  `Min`, `Max`, `Count`, `StDev`, `Var`. כל אחד חל על עמודה בודדת בלבד, ואפשר
  להפעיל אותו גם דרך הפניית יחס `Parent`/`Child`. אימתנו את זה גם באופן אמפירי:
  `Min(a, b)` עם שתי עמודות זורק `System.Data.SyntaxErrorException`:
  `"Syntax error in aggregate argument: Expecting a single column argument with
  possible 'Child' qualifier"`. לעומת זאת, `Min(a)` עם עמודה אחת מחושב כראוי
  כ-aggregate על פני הטבלה כולה — בדיוק לפי הערת התיעוד: "אם משתמשים בטבלה בודדת
  ליצירת aggregate, לא תהיה פונקציונליות group-by, וכל השורות יציגו את אותו ערך".

`Sqrt`, `Log`, `Pow` ו-`^` לא מופיעים **באף אחת מהרשימות**. זהו כל ההסבר
ל-"undefined function call": המפענח לא דוחה כאן פונקציה מוכרת שנעשה בה שימוש שגוי.
השוו זאת ל-`LEN(a)` על עמודה מספרית: `LEN` כן קיימת בדקדוק, ולכן היא זורקת שגיאה
מדויקת ושונה — `"Type mismatch in function argument: Len(), argument 1, expected
System.String"` — כי קיבלה טיפוס ארגומנט שגוי. לעומת זאת, `Sqrt`, `Log` ו-`Pow` הם
טוקנים שהמפענח פשוט מעולם לא שמע עליהם, בשום צורה. לכן השגיאה כללית ("undefined
function call") ולא ספציפית. זהו דקדוק סגור, שנבנה לסינון טבלאי, עמודות מחושבות
וצבירה בסגנון SQL על פני שורות — לא מעריך מתמטי כללי. הוא מעולם לא היה אמור "לגדול"
ולכלול `Sqrt`/`Log` רק כי נוסחה כלשהי זקוקה להם.

נקודה אחת שלא סגרנו לגמרי, ואנחנו אומרים זאת בגלוי: `Abs(x)` לא זורק שגיאה ולא
נכשל. הוא מחושב בשקט ל-`DBNull` בכל שורה, כשמפעילים אותו מחוץ להקשר aggregate של
`Parent`/`Child`. זהו מצב כשל *שונה* מהדחייה הקשה של `Sqrt`/`Log`, ו-`Abs` גם לא
מופיע בפועל בטבלת ה-aggregates של Microsoft שלמעלה. לא חקרנו לעומק למה ה-runtime
מטפל ב-`Abs` בצורה מיוחדת כזו, כי זה לא משנה לפרויקט הזה: המתרגם שלנו אף פעם לא
פולט קריאת `Abs(...)` גולמית מלכתחילה. `abs(x)` נכתב מחדש ל-`IIF((x) < 0, -(x),
(x))` בזמן התרגום (`DataColumnExpressionEmitter.cs:72`), וזה עוקף את השאלה לגמרי.
אנחנו מציינים את זה כאן במפורש, ולא מחליקים מעליו בשקט — לפי אותה רמת דיוק שהדוח
הזה שומר עליה בכל מקום אחר.

#### הפער עוקב אחרי מורכבות הנוסחה, לא אחרי תווית קטגוריה גסה

| # | נוסחה | קטגוריה | DataTable.Compute | למה |
|---|---|---|:---:|---|
| 1 | `a + b` | פשוטה | ✅ עובר | אריתמטיקה טהורה |
| 2 | `c * 2` | פשוטה | ✅ עובר | אריתמטיקה טהורה |
| 3 | `b - a` | פשוטה | ✅ עובר | אריתמטיקה טהורה |
| 4 | `d / 4` | פשוטה | ✅ עובר | אריתמטיקה טהורה |
| 5 | `(a + b) * 8` | מורכבת | ✅ עובר | אריתמטיקה טהורה |
| 6 | `sqrt(c^2 + d^2)` | מורכבת | ❌ **נכשל** | דורש `Sqrt` |
| 7 | `log(b) + c` | מורכבת | ❌ **נכשל** | דורש `Log` |
| 8 | `abs(d - b)` | מורכבת | ✅ עובר | `abs` נכתב מחדש ל-`IIF` |
| 9 | `b*2` / `b/2` אם `a>5` | מותנית | ✅ עובר | `IIF`, אריתמטיקה בלבד |
| 10 | `a+1` / `d-1` אם `b<10` | מותנית | ✅ עובר | `IIF`, אריתמטיקה בלבד |
| 11 | `1` / `0` אם `a==c` | מותנית | ✅ עובר | `IIF`, אריתמטיקה בלבד |
| 12 | `sqrt(abs((a+b)*(c-d)) + pow(a,2))` | מורכבת | ❌ **נכשל** | דורש `Sqrt` |
| 13 | `(a+b+c+d)/4` / `max(a,b)` אם `sqrt(a^2+b^2)>c` | מותנית | ❌ **נכשל** | *התנאי* דורש `Sqrt` |

שימו לב לנוסחה 13: היא שייכת לקטגוריית ה"מותנית", ונוסחאות מותנות בדרך כלל עוברות
בלי בעיה (9, 10 ו-11 כולן עוברות, כי `IIF` הוא חלק מדרגה ראשונה בדקדוק). ובכל זאת,
נוסחה 13 נכשלת — כי ה**תנאי** שלה, ולא ענפי הערך שלה, קורא ל-`sqrt`. הטקסונומיה
פשוטה/מורכבת/מותנית היא עדשה שימושית לשאלת הביצועים בסעיף 2, אבל היא לא הגבול
האמיתי של הכישלון כאן. הגבול האמיתי פשוט יותר: "האם הנוסחה קוראת ל-`sqrt` או `log`
בכל מקום — בביטוי הערך, או בתנאי". כל 4 הנוסחאות שקוראות, נכשלות. כל 9 שלא קוראות,
עוברות. הגבול הזה חד ואינפורמטיבי יותר מהאמירה "נוסחאות מורכבות נכשלות": מדובר
באופן ספציפי במתמטיקה טרנסצנדנטית ש-DataTable.Compute לא מסוגל לבטא, ללא קשר
לקטגוריה המבנית שאליה הנוסחה משתייכת מעבר לכך.

**המשמעות אינה ש-DataTable.Compute איטי יותר על נוסחאות קשות. המשמעות היא
ש-DataTable.Compute לא נחלש בהדרגה ככל שהנוסחאות מתקשות — הוא מפסיק לעבוד לגמרי,
ברגע שנוסחה זקוקה ל-`sqrt` או `log`.** מערכת תשלומים אמורה לגדול במורכבות עם
הזמן: הסכמים חדשים, חוקים חדשים, תעריפים חדשים — זו ההנחה הבסיסית של המטלה עצמה.
עבור מערכת כזו, שיטה שלא מסוגלת לבטא שורש ריבועי נפסלת מבחינת **יכולת הביטוי**,
עוד לפני שביצועיה שווים דיון בכלל. זה ממסגר מחדש את השאלה המרכזית של הפרויקט:
"האם הדירוג משתנה ככל שהנוסחאות מורכבות יותר?" גרף זמנים בלבד לא מספיק חד בשביל
לענות עליה. התשובה היא לא רק שהזמנים היחסיים משתנים (הם אכן משתנים — ראו את גרף
המורכבות בסעיף 2), אלא **שמנוע אחד נושר מהתחרות לגמרי.** הבסיס הנאיבי שנראה הכי
מהיר, על הנוסחאות שהוא בכלל מצליח להריץ, הוא לא באמת מועמד עבור מערכת שכל הרעיון
שלה הוא נוסחאות שמשתנות וגדלות.

### Python `numpy_engine.py`
כל נוסחה מתורגמת פעם אחת למחרוזת מקור של NumPy (`to_numpy_expr` ב-`formula_parser.py`).
היא מחושבת כמספר קטן של פעולות מערך וקטוריות ברמת C, על פני העמודות המלאות של
1,000,000 השורות — בלי שום לולאת Python per-row. **234 ms זמן חישוב**: מתחרה בשיטה
המהודרת ב-C#, למרות שהיא רצה בשפה מתפרשנת (interpreted). הסיבה: האריתמטיקה בפועל
מתבצעת במימוש ה-C של NumPy, לא ב-bytecode של Python. זמן הריצה הכולל שלה נשלט על
ידי שלב ה**שמירה** (13,446 ms) — אותה עלות round-trip של pyodbc שכל מנוע Python/SQL
משלם. זו לא תכונה של NumPy עצמה.

### Python `eval_engine.py` — הבסיס האיטי במכוון
זהו הבסיס השני שהמטלה מבקשת: `eval()` על אובייקט קוד שהודר מראש, נקרא בנפרד לכל
שורה — ופעמיים לשורות מותנות (פעם אחת עבור התנאי, ופעם אחת עבור הענף הנבחר).
**56,596 ms זמן חישוב** — כ-4.4 μs לכל הערכת-שורה, שני סדרי גודל איטי יותר
מ-ExpressionTrees. זה בדיוק מה שאפשר לצפות לו: כל שורה משלמת מחדש את מלוא
ה-overhead של dispatch באינטרפרטר CPython — שליפת bytecode, מניפולציית מחסנית,
וחיפושי מילון עבור `a`/`b`/`c`/`d` — ושום דבר לא מתפזר על פני השורות. זו דוגמה
קלאסית לכך שגישה של "לפרש את הנוסחה מחדש בכל שורה" לא עומדת בקנה מידה גדול.

## 2. תוצאות ההשוואה

| שיטה | נוסחאות | זמן ריצה כולל | שורות לשנייה | מקדם האצה מול האיטי ביותר |
|---|---:|---:|---:|---:|
| **C# ExpressionTrees** | 13/13 | 2,949 ms | 4,408,611 | **פי 26.2** |
| T-SQL `sp_executesql` | 13/13 | 12,260 ms | 1,060,387 | פי 6.3 |
| C# DataTable.Compute | 9/13 † | 13,018 ms | 691,358 ‡ | פי 5.9 |
| Python NumPy | 13/13 | 13,682 ms | 950,164 | פי 5.6 |
| Python `eval()` | 13/13 | 77,100 ms | 168,612 | פי 1.0 (בסיס) |

† DataTable.Compute אינו מסוגל לבטא `sqrt`/`log` — ראו סעיפים 1 ו-5. נתוני
שורות-לשנייה ומקדם ההאצה שלה מחושבים רק על 9 הנוסחאות שהיא בפועל הריצה. לכן הם
**אינם ניתנים להשוואה ישירה** מול נתוני ה-13-מתוך-13 של ארבע השיטות האחרות. הם
מופיעים כאן לשם שלמות התמונה בלבד, לא כטענת שקילות.

פילוח הידור / חישוב / שמירה (מסוכם על פני כל הנוסחאות שכל שיטה מכסה):

| שיטה | הידור | חישוב | שמירה |
|---|---:|---:|---:|
| C# ExpressionTrees | 69 ms | 844 ms | 2,036 ms |
| T-SQL `sp_executesql` | 123 ms | 5,807 ms | 6,330 ms † |
| C# DataTable.Compute | 7 ms | 10,868 ms | 2,143 ms |
| Python NumPy | 2 ms | 234 ms | 13,446 ms |
| Python `eval()` | 6 ms | 56,596 ms | 20,498 ms |

† כפי שמתועד ב-`sql/02_sp_calc_formula.sql`: שלבי החישוב והשמירה של הפרוצדורה
המאוחסנת סורקים ומחשבים מחדש את הנוסחה, כל אחד בנפרד. אין תוצאה משותפת בזיכרון
בין שאילתת ה-checksum לבין ה-`INSERT...SELECT`. לכן הפילוח הזה אינו פירוק נקי של
מעבר חישוב משותף אחד, כפי שהוא עבור ארבע המנועים האחרים. אנחנו מדווחים על כך
בכנות, במקום להציג אותו כבר-השוואה — כפי שהמטלה מבקשת.

**השמירה שולטת בזמן הריצה של כל מנוע מגובה-מסד-נתונים, חוץ מהפרוצדורה המאוחסנת
עצמה.** שמירת 10,000 שורות המדגם דרך הרשת עולה בערך שנייה-שתיים, בלי קשר למהירות
החישוב עצמו. זו בדיוק הסיבה לאסטרטגיית השמירה שתוארה בשלב 6: לחשב על פני כל מיליון
השורות, אבל לשמור רק מדגם הניתן להשוואה — במקום לכתוב לדיסק מיליון שורות × 13
נוסחאות × 5 מנועים בכל הרצה.

## 3. אימות נכונות

`scripts/compare_results.py` (מופעל דרך `run_all.py`, פלט קונסולה אמיתי —
`generatedAt` ב-`report/public/assets/results.json`):

- **ברמת השורה**: כל `(data_id, targil_id)` במדגם הדטרמיניסטי של 10,000 השורות
  מוצלב בין כל 5 השיטות בזוגות, בסף סטייה `1e-9` יחסי/מוחלט-רצפתי לפי SEMANTICS.md
  סעיף 6. **114 עברו, 0 נכשלו, 16 לא-רלוונטי** (תאי ה"לא-רלוונטי" הם בדיוק 4
  הנוסחאות ש-DataTable.Compute אינו מסוגל לבטא, כפול 4 זוגות-שיטות שכוללים אותה).
- **על כל מערך הנתונים**: checksum + null_count מתוך `t_log`, מכסה את כל 1,000,000
  השורות לכל נוסחה לכל שיטה. **114 עברו, 0 נכשלו, 16 לא-רלוונטי** (אותה תבנית).
- **תוצאה: כל 5 השיטות הניבו תוצאות זהות (סף סטייה 1e-9).**

"לא-רלוונטי" אינו ציון-עובר מוסווה. זהו מצב אמיתי, שבו DataTable.Compute אינו
מסוגל לחשב 4 מתוך 13 הנוסחאות — ואנחנו מציגים אותו ככזה, בלי להסתיר. כל השוואה
ש**יכלה** לרוץ, עברה בהצלחה.

## 4. המלצה

**עבור מערכת תשלומים אמיתית, יש להשתמש בעצי ביטוי מהודרים (Expression Trees) — או
במקבילה שלהם בסביבת הפיתוח שלכם: מטמון של נציגים (delegates) מהודרי-JIT.** מאמת
הנוסחאות (`FormulaParser`/`formula_parser.py`/`sp_pf_*`) צריך לשמש כשער חובה, לפני
שנוסחה כלשהי מגיעה לסביבת הפרודקשן — ללא קשר לבחירת מנוע החישוב.

להלן הפשרות (trade-offs), בפירוט מלא:

- **מהירות גולמית**: ExpressionTrees מנצחת פי 4.1 מעל הפרוצדורה המאוחסנת ב-T-SQL,
  ופי 26 מעל בסיס ה-`eval()` הנאיבי. אבל מהירות גולמית היא לא כל הסיפור. כל מנוע
  בשכבת האפליקציה (ארבע השיטות שאינן הפרוצדורה) עדיין משלם round-trip ברשת כדי
  לשלוף את `t_data`, ועוד round-trip כדי לשמור תוצאות. הפרוצדורה נמנעת מזה לגמרי,
  כי היא אף פעם לא עוזבת את מסד הנתונים. אם הלוגיקה העסקית שלכם כבר יושבת ליד
  הנתונים — למשל, תהליך אצווה (batch) שרץ *בתוך* שכבת מסד הנתונים — נתון השמירה של
  6,330 ms של הפרוצדורה היה נראה שונה מאוד, בלי הכפלת הסריקה המלאכותית
  (eval/persist) שמתודולוגיית ההשוואה הזו כופה עליה.
- **תחזוקתיות**: מפענח ה-recursive-descent ב-T-SQL (`sql/02_sp_calc_formula.sql`)
  הוא כ-400 שורות T-SQL שמממשות דקדוק אמיתי. הוא קשה משמעותית לקריאה, לבדיקה
  ולניפוי שגיאות, לעומת המקבילה בת כ-150 השורות ב-C# או ב-Python. צוות בלי מומחה
  T-SQL חזק ימצא את המפענח ב-C# או ב-Python קל יותר להרחבה, כאשר דקדוק הנוסחאות
  יגדל בהכרח. הנוסחאות ה"קיצוניות" שהמטלה עצמה ביקשה כבר הראו זאת: הן דחפו את
  המורכבות של מימוש ה-T-SQL גבוה יותר, באופן ברור, מכל אחת מהגרסאות בשפה המנוהלת.
- **היכן צריכה לשבת הלוגיקה העסקית?** זו השאלה האדריכלית האמיתית שמאחורי "איזו
  שיטה מנצחת". נוסחאות של מערכת תשלומים משתנות עם חוק ומדיניות, לא עם דיפלוי. לכן
  נכון לשמור אותן כנתונים (כפי שכל הפרויקט הזה עושה), ללא קשר למנוע ה*חישוב*. אבל
  **חישוב** שלהן בשכבת האפליקציה (C#/Python), ולא בשכבת מסד הנתונים, משאיר לכל
  שכבה את מה שהיא הכי טובה בו: מסד הנתונים מתמקד באחסון, בשלמות טרנזקציונית
  ובאינדוקס, והלוגיקה העסקית נשארת בשפה עם כלי בדיקה, ניפוי שגיאות וסקירת קוד
  טובים יותר מ-T-SQL. מרווח הניצחון של ExpressionTrees גדול מספיק, כך שזו אפילו לא
  פשרה בין מהירות לתחזוקתיות: היא גם המהירה ביותר וגם התחזוקתית ביותר, מבין שתי
  הבחירות הריאליות לפרודקשן — פרוצדורה מול נציג מהודר.
- **DataTable.Compute ו-`eval()` אינם מועמדים לפרודקשן.** אחד מהם לא מסוגל לבטא את
  הדקדוק המלא, והשני איטי בשני סדרי גודל בלי שום יתרון מפצה. הם מרוויחים את מקומם
  בהשוואה הזו כבסיסים שמחזקים את הטיעון עבור שלוש השיטות האחרות — לא כמתחרים
  אמיתיים.

## 5. מגבלות ומה הייתי עושה אחרת עם יותר זמן

- **פער הכיסוי של DataTable.Compute הוא אמיתי, לא קוסמטי.** מערכת פרודקשן לא יכולה
  להשתמש בו לאף נוסחה שכוללת `sqrt`, `log`, או `pow` לא-ריבועי. עם יותר זמן הייתי
  בודקת שתי אפשרויות לסגירת הפער: הרחבה בסגנון `IValueConverter` מותאמת-אישית, או
  מעבר מקדים שמחשב עמודות-עזר — כלומר, לחשב את תת-הביטויים `sqrt`/`log` בלולאת C#
  רגילה, להחזיר את התוצאות כעמודות `DataColumn` נוספות, ולתת ל-`DataColumn.Compute`
  לטפל רק בשארית האריתמטית. אבל זו ארכיטקטורה היברידית שונה מהותית, לא כוונון
  קטן, וזה היה מטשטש את ההשוואה הנקייה שמבחן הביצועים הזה עורך מול "הבסיס הנאיבי".
- **הפיצול eval/persist של הפרוצדורה המאוחסנת אינו הוגן להשוואה ישירה מול ארבעת
  המנועים האחרים**, כפי שכבר נאמר בסעיף 2. מבחן הוגן יותר לפרוצדורה היה משתמש
  ב-`SELECT INTO` או בטבלה זמנית, כדי לממש את העמודה המחושבת פעם אחת — ואז למדוד
  את ה-`INSERT` של המדגם ואת ה-checksum המלא כשתי קריאות נפרדות של אותה תוצאה
  ממומשת. זה קרוב יותר למה שארבעת המנועים האחרים עושים בפועל. בחרתי לא לעשות זאת,
  כי זה היה משנה את מה שבאמת נמדד: דפוס השימוש האמיתי בעולם של הפרוצדורה *הוא*
  "לחשב את הביטוי הזה בכל מספר פעמים שצריך את הפלט שלו". כלומר, הסריקה הכפולה
  כנה — היא לא תוצר לוואי מלאכותי של תהליך המדידה. אבל מי שקורא את הדוח צריך לזכור
  את ההסתייגות הזו, ולא לממצע את פיצול ה-eval_ms/persist_ms של הפרוצדורה מול
  הפיצולים של ארבע השיטות האחרות בלי לקחת אותה בחשבון.
- **`^`/`pow` במתרגם ה-T-SQL מתחבר משמאל במקום מימין, עבור חזקות משורשרות.**
  `a^b^c` היה מתורגם כ-`(a^b)^c`, ולא כ-`a^(b^c)` כפי שהאסוציאטיביות-מימין שהדקדוק
  מצהיר עליה דורשת. זה מעולם לא הופעל על ידי אף נוסחה בקטלוג, אבל זהו פער אמיתי
  ומוצהר בלוגיקת התרגום של `sp_pf_power`. תיקון נכון מחייב שהמתרגם יזהה שרשראות
  `^`, ויקפל אותן מימין — לפני יצירת קריאות `POWER()`.
- **גלישת גודל (magnitude overflow) של `POWER()` ב-T-SQL אינה מוגנת.** SQL Server
  זורק שגיאת גלישה אריתמטית קשה, שעוצרת את כל האצווה, עבור תוצאות `POWER()`
  שחורגות מטווח ה-`float`. בניגוד למקרה של בסיס שלילי עם מעריך שברירי, את המקרה
  הזה אי אפשר לבדוק מראש מהאופרנדים, עם `CASE WHEN` פשוט. כל נוסחה בקטלוג משתמשת
  בבסיסים ובמעריכים קטנים, כך שזה מעולם לא מופעל בפועל. אבל מערכת פרודקשן שמקבלת
  מעריכים שרירותיים הייתה זקוקה לבדיקת-גודל מראש, או ל-`TRY/CATCH` per-row —
  שמוותר לגמרי על יתרון הביצועים מבוסס-הקבוצות.
- **C# מכוון ל-.NET 10, לא ל-.NET 8** כפי שהמטלה המקורית ציינה — רק .NET 10
  SDK/runtime היו זמינים בסביבה הזו; שינוי היעד היה חריגה מוצהרת ומאושרת על ידי
  המשתמש, לא שינוי בשקט (ראו היסטוריית git).
- **מדגם 10,000 השורות נבחר אקראית, לא באופן מדורג (stratified).** הוא גדול מספיק
  — 1% מהמערך — כך שבפועל שני הענפים של כל נוסחה מותנית מיוצגים היטב. זה מאומת דרך
  הפיצול של כ-50/50 שמדווח `seed_data.py`. אבל נוסחה שתנאי הענף שלה נדיר (למשל,
  אמיתי עבור 0.01% מהשורות בלבד) הייתה עלולה להיות בתת-ייצוג בהשוואה השמורה — גם אם
  בדיקת ה-checksum על כל מערך הנתונים עדיין הייתה תופסת כל אי-התאמה אמיתית. עם יותר
  זמן, הייתי מדגמת בשכבות (stratified) לפי ענף של כל נוסחה מותנית, כדי להבטיח ששני
  הענפים מיוצגים באותו יחס כמו במערך המלא.
- **אין בדיקת עומס או concurrency.** כל מספר כאן הוא הרצה חד-thread, חיבור בודד.
  מערכת תשלומים אמיתית שמחשבת נוסחאות תחת עומס במקביל (קריאות מקבילות לפרוצדורה,
  תחרות על pool של חיבורים) הייתה מציגה תמונה שונה, במיוחד עבור המנוע בשכבת מסד
  הנתונים.
