# REPORT.he.md — השוואת ביצועים להערכת נוסחאות דינמיות

הנתונים שלהלן נלקחו מהרצה בודדת של `python run_all.py` מול מסד נתונים נקי (seed=42,
1,000,000 שורות, 13 נוסחאות, מדגם דטרמיניסטי של 10,000 שורות). לשחזור העצמאי שלהם ראו
README.md. זהו התרגום העברי המלא של REPORT.md — לא תרגום מכונה, אלא ניסוח טכני עצמאי
באותה רמת דיוק.

## 1. חמש השיטות, ומדוע כל אחת מתפקדת כפי שהיא מתפקדת

### C# `ExpressionTrees` — השיטה המנצחת
כל נוסחה מפוענחת פעם אחת (מפענח recursive-descent שנכתב ידנית, ראו
`src/dotnet/PaymentSystem.Shared/FormulaParser.cs`) לעץ תחביר קטן (AST), ואז ה-AST הזה
מהודר פעם אחת לעץ `System.Linq.Expressions` ומתקמפל (JIT) לנציג (delegate) native מסוג
`Func<double,double,double,double,double?>`. כל שורה לאחר מכן היא קריאת פונקציה native
ישירה — בלי פענוח מחרוזת מחדש, בלי dispatch של אינטרפרטר, בלי boxing מעבר לערך המוחזר
הניתן-לריקון (nullable). זו בדיוק אסטרטגיית "הידור פעם אחת, הרצה מיליון פעמים" שהמטלה
מבקשת, והמספרים מראים למה זו המנצחת: **844 ms זמן חישוב עבור 13,000,000 הערכות-שורה**
(13 נוסחאות × 1M שורות), כלומר פחות מ-65 ns לשורה, כולל קריאת פונקציה, כמה פעולות
אריתמטיות ובדיקת NaN/אינסוף.

### T-SQL `sp_executesql`
חישוב מבוסס-קבוצות (set-based) שמתבצע כולו בתוך מנוע מסד הנתונים: הנוסחה מתורגמת
לביטוי SQL בודד (עם שומרי תחום `CASE WHEN`/`NULLIF` שמוכנסים בזמן התרגום — ראו סעיף 3)
ומחושבת על כל 1,000,000 השורות ב-`SELECT` אחד. אף שורה לא חוצה את הרשת בנפרד. השיטה
הזו מתחרה טוב מול ExpressionTrees בזמן החישוב לנוסחה, אך מפסידה בשלב ה**שמירה** (6,330
ms בסך הכול), כי בניגוד לארבע השיטות האחרות אין לה מערך תוצאות בזיכרון לשימוש חוזר —
כל שמירה היא סריקה מלאה נוספת של הטבלה, שמחשבת את הנוסחה מחדש מאפס (ראו סעיף 4). זו גם
השיטה היחידה שלא משלמת שום עלות העברת נתונים עבור חישוב ה-checksum על כל מערך הנתונים,
מכיוון שהחישוב הזה מתבצע במקום שבו הנתונים כבר נמצאים.

### C# `DataTable.Compute`
"הבסיס הנאיבי" שהמטלה מבקשת במפורש — וההצגה שלו חלשה מהצפוי, באופן שמעניין יותר מסתם
"איטי". מתברר שלשפת הביטויים של `System.Data.DataColumn` **אין בכלל `Sqrt`, `Log`
או `Pow`/`^`**. ארבע מתוך שלוש-עשרה הנוסחאות (6, 7, 12, 13 — כל מה שדורש `sqrt` או
`log`, בכל מקום, כולל בתוך תנאי) **בלתי ניתנות לביטוי** במנוע הזה, ומדולגות עם סיבה
מתועדת, ולא מזויפות בשקט — ראו את הראיות וההסבר המלא בהמשך. עבור תשע הנוסחאות שהוא כן
יכול לבטא (`abs`/`min`/`max` נכתבות מחדש כביטויי `IIF`, ו-`x^2` נכתב מחדש כ-`x*x`
מכיוון שכל חזקה בקטלוג היא בפועל המספר הליטרלי 2), כל קריאת `DataColumn.Compute`
מפענחת את מחרוזת הביטוי מאפס — **10,868 ms זמן חישוב**, פי 13 בערך יותר איטי לשורה
מ-ExpressionTrees, כי אין שלב הידור שמפזר את העלות על פני שורות רבות.

#### הראיות: השגיאה של ה-runtime עצמו, לא הפרשנות שלנו לה

לא הסקנו את זה מתיעוד — הזנו ל-runtime של .NET את הצורה המתורגמת המדויקת של הנוסחאות
הכושלות, ותפסנו מה שהוא זרק בפועל. כדי לשלול שמדובר בבאג בתרגום שלנו (למשל, אותיות
רישיות שגויות), נבדקה כל וריאציה של אותיות רישיות/תחביר שאדם סביר היה מנסה:

```
Sqrt(c * c + d * d)   -> System.Data.EvaluateException:
                          "The expression contains undefined function call Sqrt()."
sqrt(c * c + d * d)   -> System.Data.EvaluateException:
                          "The expression contains undefined function call sqrt()."
SQRT(c * c + d * d)   -> System.Data.EvaluateException:
                          "The expression contains undefined function call SQRT()."
sQrT(c * c + d * d)   -> System.Data.EvaluateException:
                          "The expression contains undefined function call sQrT()."
Sqrt (c * c + d * d)  -> System.Data.EvaluateException:
                          "The expression contains undefined function call Sqrt()."   (רווח לפני הסוגר)
Math.Sqrt(c)          -> System.Data.EvaluateException:
                          "The expression contains undefined function call Math.Sqrt()."
Log(c) / log(c) / LOG(c) / lOg(c)  -> אותה תבנית, "undefined function call {as-typed}()."
Pow(a, 2) / pow(a, 2) -> System.Data.EvaluateException:
                          "The expression contains undefined function call Pow()."
a ^ 2                 -> System.Data.EvaluateException:
                          "The expression contains unsupported operator '^'."
```

כל וריאציה — כל אות רישית, עם או בלי רווח לפני הסוגר, ארגומנט בודד או מרובה, אפילו
עם namespace מוקדם (`Math.Sqrt`) — נכשלת באופן זהה. סוג החריגה הוא הטיפוס
`System.Data.EvaluateException` של .NET עצמו, שנזרק מתוך מפענח הביטויים של
`System.Data.dll`; שום דבר בקוד שלנו לא זורק אותו. זה שולל באג בתרגום שלנו (אותיות
רישיות שגויות, תחביר שגוי): הפונקציה פשוט לא קיימת בדקדוק, בשום איות.

בקוד שנשלח בפועל, הידע הזה מקודד כשומר יזום ולא כתפיסת חריגה בזמן ריצה — המתרגם דוחה
`sqrt`/`log`/`pow` לא-ריבועי עוד לפני בניית `DataColumn` כלשהו, כך שה-`EvaluateException`
הגולמי שלמעלה אף פעם לא באמת עולה בזמן ריצה; זו הראיה שהצדיקה את כתיבת השומר, לא משהו
שהקוד השלוח תופס ישירות:

- **מיקום זריקת השומר**: `src/dotnet/DataTableCompute/DataColumnExpressionEmitter.cs:78-81`
  (`sqrt`) ו-`:76-77`/`:66-67` (`pow`/חזקה לא-ריבועית) — כל אחד זורק את
  `NotSupportedByDataTableComputeException` שלנו עם הודעה שמציינת את הפונקציה החסרה.
- **מיקום דילוג הנוסחה**: `src/dotnet/DataTableCompute/Program.cs:60-65` — מנוע ההרצה
  תופס את החריגה הזו לכל נוסחה, רושם אותה, מדפיס `SKIPPED -- {הודעה}`, וממשיך לנוסחה
  הבאה במקום לעצור את כל ההרצה או לזייף תוצאה.

לשחזור עצמאי: `dotnet run --project src/dotnet/DataTableCompute -c Release` — וקריאת
שורות ה-`SKIPPED` בפלט הקונסולה שלו ישירות.

#### מדוע זה קורה: `DataColumn.Expression` אינה שפת ביטויים כללית

לפי התיעוד הרשמי של Microsoft עבור `DataColumn.Expression`
([learn.microsoft.com/dotnet/api/system.data.datacolumn.expression](https://learn.microsoft.com/en-us/dotnet/api/system.data.datacolumn.expression)),
המאפיין קיים כדי לעשות בדיוק שלושה דברים: "לסנן שורות, לחשב את הערכים בעמודה, או ליצור
עמודת aggregate". הדקדוק שלו הוא קבוצה קטנה וסגורה, לא ספריית מתמטיקה הניתנת להרחבה:

- **אופרטורים**: השוואה (`< > <= >= = IN LIKE`), חיבור בוליאני (`AND OR NOT`),
  אריתמטיקה (`+ - * / %`), שרשור מחרוזות (`+`).
- **פונקציות** (בדיוק שש, לפי סעיף "Functions" בתיעוד עצמו): `CONVERT`, `LEN`,
  `ISNULL`, `IIF`, `TRIM`, `SUBSTRING`.
- **Aggregates** (בדיוק שבעה, לפי סעיף "Aggregates" בתיעוד עצמו): `Sum`, `Avg`,
  `Min`, `Max`, `Count`, `StDev`, `Var` — כל אחד חל על עמודה בודדת בלבד, אופציונלית
  דרך הפניית יחס `Parent`/`Child`. אימתנו את זה גם באופן אמפירי: `Min(a, b)` (שתי
  עמודות) זורק `System.Data.SyntaxErrorException`:
  `"Syntax error in aggregate argument: Expecting a single column argument with
  possible 'Child' qualifier"`, בעוד ש-`Min(a)` (עמודה אחת) מחושב כראוי כ-aggregate
  על פני הטבלה כולה, בדיוק לפי הערת התיעוד ש"אם משתמשים בטבלה בודדת ליצירת
  aggregate, לא תהיה פונקציונליות group-by — כל השורות יציגו את אותו ערך".

`Sqrt`, `Log`, `Pow` ו-`^` לא מופיעים **באף אחת מהרשימות**. זהו כל ההסבר ל-"undefined
function call": המפענח לא דוחה פונקציה מוכרת שנעשה בה שימוש שגוי (השוו ל-`LEN(a)` על
עמודה מספרית, שכן מוכרת וזורקת שגיאה מדויקת ושונה — `"Type mismatch in function
argument: Len(), argument 1, expected System.String"` — כי `LEN` קיימת בדקדוק
וקיבלה טיפוס ארגומנט שגוי). `Sqrt`/`Log`/`Pow` הם טוקנים שהמפענח פשוט מעולם לא שמע
עליהם, בשום צורה, ולכן השגיאה כללית ("undefined function call") ולא ספציפית. זהו
דקדוק סגור שנבנה לסינון טבלאי, עמודות מחושבות וצבירה בסגנון SQL על פני שורות — לא
מעריך מתמטי כללי, והוא מעולם לא היה עומד "לגדול" ולכלול `Sqrt`/`Log` רק כי נוסחה
כלשהי זקוקה להם.

קצה חוט אחד ביושר: `Abs(x)` לא זורק שגיאה ולא נכשל — הוא מחושב בשקט ל-`DBNull` בכל
שורה כשמפעילים אותו מחוץ להקשר aggregate של `Parent`/`Child`, שזהו מצב כשל *שונה*
מהדחייה הקשה של `Sqrt`/`Log`, ו-`Abs` גם לא מופיע בפועל בטבלת ה-aggregates של
Microsoft שלמעלה. לא חקרנו לעומק למה ה-runtime מטפל ב-`Abs` באופן מיוחד כזה, כי זה
לא משנה לפרויקט הזה: המתרגם שלנו אף פעם לא פולט קריאת `Abs(...)` גולמית מלכתחילה —
`abs(x)` נכתב מחדש ל-`IIF((x) < 0, -(x), (x))` בזמן התרגום
(`DataColumnExpressionEmitter.cs:72`), מה שעוקף את השאלה לגמרי. מסומן כאן במפורש
במקום מוחלק בשקט, לפי אותה רמת דיוק שהדוח הזה מחזיק את עצמו אליה בכל מקום אחר.

#### הפער עוקב אחרי מורכבות הנוסחה, לא אחרי תווית קטגוריה גסה

| # | נוסחה | קטגוריה | DataTable.Compute | למה |
|---|---|---|:---:|---|
| 1 | `a + b` | פשוטה | ✅ עובר | אריתמטיקה טהורה |
| 2 | `c * 2` | פשוטה | ✅ עובר | אריתמטיקה טהורה |
| 3 | `b - a` | פשוטה | ✅ עובר | אריתמטיקה טהורה |
| 4 | `d / 4` | פשוטה | ✅ עובר | אריתמטיקה טהורה |
| 5 | `(a + b) * 8` | מורכבת | ✅ עובר | אריתמטיקה טהורה |
| 6 | `sqrt(c^2 + d^2)` | מורכבת | ❌ **נכשל** | דורש `Sqrt` |
| 7 | `log(b) + c` | מורכבת | ❌ **נכשל** | דורש `Log` |
| 8 | `abs(d - b)` | מורכבת | ✅ עובר | `abs` נכתב מחדש ל-`IIF` |
| 9 | `b*2` / `b/2` אם `a>5` | מותנית | ✅ עובר | `IIF`, אריתמטיקה בלבד |
| 10 | `a+1` / `d-1` אם `b<10` | מותנית | ✅ עובר | `IIF`, אריתמטיקה בלבד |
| 11 | `1` / `0` אם `a==c` | מותנית | ✅ עובר | `IIF`, אריתמטיקה בלבד |
| 12 | `sqrt(abs((a+b)*(c-d)) + pow(a,2))` | מורכבת | ❌ **נכשל** | דורש `Sqrt` |
| 13 | `(a+b+c+d)/4` / `max(a,b)` אם `sqrt(a^2+b^2)>c` | מותנית | ❌ **נכשל** | *התנאי* דורש `Sqrt` |

שימו לב לנוסחה 13: היא בקטגוריית ה"מותנית", וברירת המחדל היא שנוסחאות מותנות עוברות
בלי בעיה (9, 10, 11 כולן עוברות — `IIF` הוא חלק מדרגה ראשונה בדקדוק). 13 נכשלת בכל
זאת, כי ה**תנאי** שלה, ולא ענפי הערך שלה, קורא ל-`sqrt`. הטקסונומיה
פשוטה/מורכבת/מותנית היא עדשה שימושית לשאלת הביצועים בסעיף 2, אבל היא לא גבול הכישלון
האמיתי כאן — הגבול האמיתי הוא בדיוק "האם הנוסחה הזו קוראת ל-`sqrt` או `log` בכל
מקום, בביטוי הערך *או* בתנאי". כל 4 הנוסחאות שכן, נכשלות. כל 9 שלא, עוברות. הגבול הזה
חד ואינפורמטיבי יותר מ-"נוסחאות מורכבות נכשלות": מדובר ספציפית במתמטיקה
טרנסצנדנטית ש-DataTable.Compute לא מסוגל לבטא, ללא קשר לאיזו קטגוריה מבנית הנוסחה
משתייכת אליה מלבד זאת.

**המשמעות אינה "DataTable.Compute איטי יותר על נוסחאות קשות" — אלא ש-DataTable.Compute
לא נחלש בהדרגה ככל שהנוסחאות מתקשות. הוא מפסיק לעבוד, לגמרי, ברגע שנוסחה זקוקה
ל-`sqrt` או `log`.** עבור מערכת תשלומים שקטלוג הנוסחאות שלה צפוי לגדול במורכבות עם
הזמן (הסכמים חדשים, חוקים חדשים, תעריפים חדשים — כפי שההנחה הבסיסית של המטלה עצמה
קובעת), שיטה שלא מסוגלת לבטא שורש ריבועי נפסלת מבחינת **יכולת הביטוי**, עוד לפני
שביצועיה שווים דיון בכלל. זה ממסגר מחדש את השאלה המרכזית של הפרויקט — "האם הדירוג
משתנה ככל שהנוסחאות מורכבות יותר?" — בצורה חדה יותר מגרף זמנים לבדו: התשובה היא לא
רק שהזמנים היחסיים משתנים (הם משתנים, ראו את גרף המורכבות בסעיף 2), אלא **שמנוע אחד
נושר מהתחרות לגמרי.** הבסיס הנאיבי שנראה המהיר ביותר, על הנוסחאות שהוא בכלל מריץ, הוא
לא באמת מועמד עבור מערכת שכל הרעיון שלה הוא נוסחאות שמשתנות וגדלות.

### Python `numpy_engine.py`
כל נוסחה מתורגמת פעם אחת למחרוזת מקור של NumPy (`to_numpy_expr` ב-`formula_parser.py`),
ואז מחושבת כמספר קטן של פעולות מערך וקטוריות ברמת C, על פני העמודות המלאות של
1,000,000 השורות — בלי לולאת Python per-row בכלל. **234 ms זמן חישוב**, מתחרה בשיטה
המהודרת ב-C# למרות ריצה בשפה מתורגמת (interpreted), כי החישוב האריתמטי בפועל מתבצע
במימוש ה-C של NumPy, לא ב-bytecode של Python. זמן הריצה הכולל שלה נשלט על ידי שלב
ה**שמירה** (13,446 ms) — אותה עלות round-trip של pyodbc שכל מנוע Python/SQL משלם, ולא
תכונה של NumPy עצמה.

### Python `eval_engine.py` — הבסיס האיטי במכוון
הבסיס השני שהמטלה מבקשת: `eval()` על אובייקט קוד שהודר מראש, נקרא פעם אחת לכל שורה,
ופעמיים לשורות מותנות (פעם אחת עבור התנאי, ופעם אחת עבור הענף הנבחר). **56,596 ms זמן
חישוב** — כ-4.4 μs לכל הערכת-שורה, שני סדרי גודל איטי יותר מ-ExpressionTrees. זה בדיוק
מה שהיינו מצפים לו: כל שורה משלמת overhead מלא של dispatch באינטרפרטר CPython (שליפת
bytecode, מניפולציית מחסנית, חיפושי מילון עבור `a`/`b`/`c`/`d`) בלי שום דבר שמתפזר על
פני השורות. זו הדגמה ספרותית לכך ש"לפרש את הנוסחה מחדש בכל פעם" לא מתמודד עם קנה מידה.

## 2. תוצאות ההשוואה

| שיטה | נוסחאות | זמן ריצה כולל | שורות לשנייה | מקדם האצה מול האיטי ביותר |
|---|---:|---:|---:|---:|
| **C# ExpressionTrees** | 13/13 | 2,949 ms | 4,408,611 | **פי 26.2** |
| T-SQL `sp_executesql` | 13/13 | 12,260 ms | 1,060,387 | פי 6.3 |
| C# DataTable.Compute | 9/13 † | 13,018 ms | 691,358 ‡ | פי 5.9 |
| Python NumPy | 13/13 | 13,682 ms | 950,164 | פי 5.6 |
| Python `eval()` | 13/13 | 77,100 ms | 168,612 | פי 1.0 (בסיס) |

† DataTable.Compute אינו מסוגל לבטא `sqrt`/`log` — ראו סעיפים 1 ו-5. נתוני שורות-לשנייה
ומקדם ההאצה שלה מחושבים רק על 9 הנוסחאות שהיא באמת הריצה, כך שהם **אינם ניתנים להשוואה
ישירה** מול נתוני ה-13-מתוך-13 של ארבע השיטות האחרות — מובאים לשם שלמות התמונה, לא
כטענת שקילות.

פילוח הידור / חישוב / שמירה (מסוכם על פני כל הנוסחאות שכל שיטה מכסה):

| שיטה | הידור | חישוב | שמירה |
|---|---:|---:|---:|
| C# ExpressionTrees | 69 ms | 844 ms | 2,036 ms |
| T-SQL `sp_executesql` | 123 ms | 5,807 ms | 6,330 ms † |
| C# DataTable.Compute | 7 ms | 10,868 ms | 2,143 ms |
| Python NumPy | 2 ms | 234 ms | 13,446 ms |
| Python `eval()` | 6 ms | 56,596 ms | 20,498 ms |

† כפי שמתועד ב-`sql/02_sp_calc_formula.sql`: שלבי החישוב והשמירה של הפרוצדורה
המאוחסנת סורקים ומחשבים מחדש את הנוסחה כל אחד בנפרד (אין תוצאה משותפת בזיכרון בין
שאילתת ה-checksum לבין ה-`INSERT...SELECT`), כך שהפילוח הזה אינו פירוק נקי של מעבר
חישוב משותף אחד, כפי שהוא עבור ארבע המנועים האחרים. מדווח בכנות במקום להיות מוצג
כבר-השוואה, כפי שהמטלה מבקשת.

**השמירה שולטת בזמן הריצה של כל מנוע מגובה-מסד-נתונים חוץ מהפרוצדורה המאוחסנת
עצמה** — שמירת 10,000 שורות המדגם דרך הרשת עולה בערך שנייה-שתיים בלי קשר למהירות
החישוב עצמו, וזו בדיוק הסיבה לאסטרטגיית השמירה שתוארה בשלב 6 (לחשב על פני כל 1M
השורות, לשמור רק מדגם הניתן להשוואה) במקום לכתוב 1M שורות × 13 נוסחאות × 5 מנועים
לדיסק בכל הרצה.

## 3. אימות נכונות

`scripts/compare_results.py` (מופעל דרך `run_all.py`, פלט קונסולה אמיתי —
`generatedAt` ב-`report/public/assets/results.json`):

- **ברמת השורה**: כל `(data_id, targil_id)` במדגם הדטרמיניסטי של 10,000 השורות,
  מוצלב בין כל 5 השיטות בזוגות, בסף סטייה `1e-9` יחסי/מוחלט-רצפתי לפי SEMANTICS.md
  סעיף 6. **114 עברו, 0 נכשלו, 16 לא-רלוונטי** (תאי ה"לא-רלוונטי" הם בדיוק 4 הנוסחאות
  ש-DataTable.Compute אינו מסוגל לבטא, כפול 4 זוגות-שיטות שכוללים אותה).
- **על כל מערך הנתונים**: checksum + null_count מתוך `t_log`, מכסה את כל 1,000,000
  השורות לכל נוסחה לכל שיטה. **114 עברו, 0 נכשלו, 16 לא-רלוונטי** (אותה תבנית).
- **תוצאה: כל 5 השיטות הניבו תוצאות זהות (סף סטייה 1e-9).**

"לא-רלוונטי" אינו ציון-עובר מוסווה — זהו מצב אמיתי שבו DataTable.Compute אינו מסוגל
לחשב 4 מתוך 13 הנוסחאות, המוצג ככזה ולא מוסתר. כל השוואה ש**יכלה** לרוץ, עברה.

## 4. המלצה

**להשתמש בעצי ביטוי מהודרים (Expression Trees) — או המקבילה שלהם בסביבת הפיתוח שלכם,
מטמון של נציגים (delegates) מהודרי-JIT — עבור מערכת תשלומים אמיתית**, כאשר מאמת
הנוסחאות (`FormulaParser`/`formula_parser.py`/`sp_pf_*`) משמש כשער חובה לפני שנוסחה
כלשהי מגיעה לסביבת הפרודקשן, ללא קשר לבחירת מנוע החישוב.

להלן הפשרות (trade-offs), בפירוט מלא:

- **מהירות גולמית**: ExpressionTrees מנצחת פי 4.1 מעל הפרוצדורה המאוחסנת ב-T-SQL
  ופי 26 מעל בסיס ה-`eval()` הנאיבי. אבל מהירות גולמית אינה כל הסיפור — כל מנוע
  בשכבת האפליקציה (ארבע השיטות שאינן הפרוצדורה) עדיין משלם round-trip ברשת כדי לשלוף
  את `t_data`, ועוד round-trip כדי לשמור תוצאות — דבר שהפרוצדורה נמנעת ממנו לגמרי,
  כיוון שהיא אף פעם לא עוזבת את מסד הנתונים. אם הלוגיקה העסקית שלכם כבר יושבת ליד
  הנתונים (למשל, תהליך אצווה (batch) שרץ *בתוך* שכבת מסד הנתונים), נתון השמירה של
  6,330 ms של הפרוצדורה היה נראה שונה מאוד בלי הכפלת הסריקה המלאכותית (eval/persist)
  שמתודולוגיית ההשוואה הזו כופה עליה.
- **תחזוקתיות**: מפענח ה-recursive-descent ב-T-SQL (`sql/02_sp_calc_formula.sql`) הוא
  כ-400 שורות T-SQL שמממשות דקדוק אמיתי — קשה משמעותית לקריאה, לבדיקה ולניפוי שגיאות
  מהמקבילה בת כ-150 השורות ב-C# או ב-Python. צוות בלי מומחה T-SQL חזק ימצא את המפענח
  ב-C# או ב-Python קל יותר להרחבה כאשר דקדוק הנוסחאות יגדל בהכרח (הנוסחאות ה"קיצוניות"
  שהמטלה עצמה ביקשה כבר דחפו את המורכבות של מימוש ה-T-SQL גבוה יותר בבירור מכל אחת
  מהגרסאות בשפה המנוהלת).
- **היכן צריכה לשבת הלוגיקה העסקית?** זו השאלה האדריכלית האמיתית מאחורי "איזו שיטה
  מנצחת". נוסחאות של מערכת תשלומים משתנות עם חוק ומדיניות, לא עם דיפלוי — לשמור אותן
  כנתונים (כפי שכל הפרויקט הזה עושה) נכון ללא קשר למנוע ה*חישוב*. אבל *חישוב* שלהן
  בשכבת האפליקציה (C#/Python) ולא בשכבת מסד הנתונים משאיר את מסד הנתונים עושה את מה
  שהוא הכי טוב בו (אחסון, שלמות טרנזקציונית, אינדוקס), ומשאיר את הלוגיקה העסקית בשפה
  עם כלי בדיקה, ניפוי שגיאות וסקירת קוד טובים יותר מ-T-SQL. מרווח הניצחון של
  ExpressionTrees גדול מספיק שזו אפילו לא פשרה בין מהירות לתחזוקתיות כאן — זו גם
  המהירה ביותר וגם התחזוקתית ביותר מבין שתי הבחירות הריאליות לפרודקשן (פרוצדורה מול
  נציג מהודר).
- **DataTable.Compute ו-`eval()` אינם מועמדים לפרודקשן** — אחד לא מסוגל לבטא את
  הדקדוק המלא, השני איטי בשני סדרי גודל בלי יתרון מפצה. הם מרוויחים את מקומם בהשוואה
  הזו כבסיסים שמבססים את הטיעון עבור שלוש השיטות האחרות, לא כמתחרים.

## 5. מגבלות ומה הייתי עושה אחרת עם יותר זמן

- **פער הכיסוי של DataTable.Compute הוא אמיתי, לא קוסמטי.** מערכת פרודקשן לא יכולה
  להשתמש בו לאף נוסחה שכוללת `sqrt`/`log`/`pow` לא-ריבועי. עם יותר זמן הייתי בודקת
  אם הרחבה בסגנון `IValueConverter` מותאמת-אישית, או מעבר מקדים שמחשב עמודות-עזר
  (חישוב תת-ביטויי `sqrt`/`log` בלולאת C# רגילה, החזרת התוצאות כעמודות `DataColumn`
  נוספות, ואז מתן ל-`DataColumn.Compute` לטפל רק בשארית האריתמטית) יכולים לסגור את
  הפער — אבל זו ארכיטקטורה היברידית שונה מהותית, לא כוונון קטן, וזה היה מטשטש את
  ההשוואה הנקייה שמבחן הביצועים הזה עורך מול "הבסיס הנאיבי".
- **הפיצול eval/persist של הפרוצדורה המאוחסנת אינו השוואת "תפוחים לתפוחים"**, כפי
  שנאמר בסעיף 2. מבחן הוגן יותר לפרוצדורה היה משתמש ב-`SELECT INTO` או בטבלה זמנית
  כדי לממש את העמודה המחושבת פעם אחת, ואז למדוד את ה-`INSERT` של המדגם ואת ה-checksum
  המלא כשתי קריאות של אותה תוצאה ממומשת — קרוב יותר למה שארבעת המנועים האחרים עושים.
  בחרתי לא לעשות זאת כי זה היה משנה את מה שבאמת נמדד (דפוס השימוש האמיתי-בעולם של
  הפרוצדורה *הוא* "לחשב את הביטוי הזה בכל מספר פעמים שצריך את הפלט שלו", כך שהסריקה
  הכפולה כנה, ולא תוצר לוואי מלאכותי של תהליך המדידה) — אבל קורא לא צריך לממוצע את
  פיצול ה-eval_ms/persist_ms שלה מול הפיצולים של ארבע השיטות האחרות בלי ההסתייגות הזו
  בראש.
- **`^`/`pow` במתרגם ה-T-SQL מתחבר משמאל במקום מימין עבור חזקות משורשרות**
  (`a^b^c` היה מתורגם כ-`(a^b)^c`, לא `a^(b^c)` לפי האסוציאטיביות-מימין שהדקדוק
  מצהיר עליה) — מעולם לא הופעל על ידי אף נוסחה בקטלוג, אבל זהו פער אמיתי ומוצהר
  בלוגיקת התרגום של `sp_pf_power`. תיקון נכון מחייב שהמתרגם יזהה שרשראות `^` ויקפל
  אותן מימין, לפני יצירת קריאות `POWER()`.
- **גלישת גודל (magnitude overflow) של `POWER()` ב-T-SQL אינה מוגנת.** SQL Server
  זורק שגיאת גלישה אריתמטית קשה, שעוצרת את כל האצווה, עבור תוצאות `POWER()` שחורגות
  מטווח ה-`float` — ובניגוד למקרה של בסיס שלילי עם מעריך שברירי, את זה אי אפשר לבדוק
  מראש מהאופרנדים עם `CASE WHEN` פשוט. כל נוסחה בקטלוג משתמשת בבסיסים/מעריכים קטנים,
  כך שזה מעולם לא מופעל בפועל; מערכת פרודקשן שמקבלת מעריכים שרירותיים הייתה זקוקה
  לבדיקת-גודל מראש או ל-`TRY/CATCH` per-row (שמוותר לגמרי על יתרון הביצועים
  מבוסס-הקבוצות).
- **C# מכוון ל-.NET 10, לא ל-.NET 8** כפי שהמטלה המקורית ציינה — רק .NET 10
  SDK/runtime היו זמינים בסביבה הזו; שינוי היעד היה חריגה מוצהרת ומאושרת על ידי
  המשתמש, לא שינוי בשקט (ראו היסטוריית git).
- **מדגם 10,000 השורות נבחר אקראית, לא באופן מדורג (stratified).** הוא גדול מספיק
  (1% מהמערך) כך שבפועל שני הענפים של כל נוסחה מותנית מיוצגים היטב (מאומת דרך
  הפיצול של כ-50/50 שמדווח על ידי `seed_data.py`), אבל נוסחה שתנאי הענף שלה נדיר
  (נניח, אמיתי עבור 0.01% מהשורות) הייתה עלולה להיות בתת-ייצוג בהשוואה השמורה, אף
  שבדיקת ה-checksum על כל מערך הנתונים עדיין הייתה תופסת אי-התאמה אמיתית. עם יותר
  זמן הייתי מדגמת בשכבות (stratified) לפי ענף של כל נוסחה מותנית, כדי להבטיח ששני
  הענפים מיוצגים באותו יחס כמו במערך המלא.
- **אין בדיקת עומס או concurrency.** כל מספר כאן הוא הרצה חד-thread, חיבור בודד.
  מערכת תשלומים אמיתית שמחשבת נוסחאות תחת עומס במקביל (קריאות מקבילות לפרוצדורה,
  תחרות על pool של חיבורים) הייתה מציגה תמונה שונה, במיוחד עבור המנוע בשכבת מסד
  הנתונים.
